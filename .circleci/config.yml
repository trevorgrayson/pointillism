version: 2.1

orbs:
  python: circleci/python@0.2.1

jobs:
  build-and-test:
    executor: python/default
    steps:
      - checkout
      - run: sudo apt install graphviz build-essential libldap2-dev libsasl2-dev
python3-dev fonts-freefont-ttf
      - run:
          command: make test
          name: Test

workflows:
  main:
    jobs:
      - build-and-test
